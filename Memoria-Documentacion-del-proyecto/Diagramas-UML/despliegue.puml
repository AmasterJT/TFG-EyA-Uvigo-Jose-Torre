@startuml
title Diagrama de despliegue - Sistema completo

node "PC Usuario\n(Windows)" as PC {
  artifact "Desktop App (Java)" as Desktop
  folder "Ficheros:\n\t-PDF (etiquetas)\n\t-Excel" as Files
}

node "Servidor BD" as Server {
  database "MySQL" as MySQL
}

component "Gestion de roles" as RolesManager

node "Impresora" as Printer

Desktop --> RolesManager : JDBC / SQL
RolesManager --> MySQL: JDBC / SQL
MySQL --> Desktop
Desktop --> Files : Exporta PDF/Excel
Files --> Printer : Imprime etiquetas

' (Opcional si tu arquitectura real incluye Android + API)
node "Móvil Android" as Phone {
  artifact "Android App" as Android
}
node "Servidor API" as ApiServer {
  artifact "Backend REST (Spring Boot)" as API
}

Android --> API : HTTPS / JSON
API --> MySQL : JDBC / SQL
MySQL --> API
API --> Android

@enduml
